(() => {
  'use strict';
  if (document.getElementById('liftProductOffcanvasStyles')) return;
  const style=document.createElement('style');
  style.id='liftProductOffcanvasStyles';
  style.textContent=`
    .card,[class*="product-card"]{transition:transform .22s ease,box-shadow .22s ease!important;cursor:pointer;will-change:transform}.card:hover,[class*="product-card"]:hover{transform:translateY(-5px) scale(1.015);box-shadow:0 12px 28px rgba(0,0,0,.14)!important;z-index:2}.card .pic img,[class*="product-card"] img{transition:transform .28s ease!important}.card:hover .pic img,[class*="product-card"]:hover img{transform:scale(1.045)}
    #liftProductBackdrop{position:fixed;inset:0;background:rgba(0,0,0,.48);opacity:0;visibility:hidden;transition:.25s;z-index:9998;backdrop-filter:blur(3px)}#liftProductBackdrop.open{opacity:1;visibility:visible}
    #liftProductOffcanvas{position:fixed;left:50%;top:50%;width:min(1080px,92vw);max-height:90dvh;background:#fff;border-radius:20px;transform:translate(-50%,-46%) scale(.96);opacity:0;visibility:hidden;transition:transform .28s cubic-bezier(.2,.8,.2,1),opacity .22s;z-index:9999;box-shadow:0 28px 80px rgba(0,0,0,.28);overflow:hidden;display:flex;flex-direction:column}#liftProductOffcanvas.open{transform:translate(-50%,-50%) scale(1);opacity:1;visibility:visible}.lift-product-panel-head{position:absolute;top:12px;right:14px;z-index:8}.lift-product-panel-head strong{display:none}#liftProductClose{width:42px;height:42px;border-radius:50%;border:1px solid #e2e2e2;background:#fff;font-size:26px;cursor:pointer;display:flex;align-items:center;justify-content:center;box-shadow:0 4px 14px rgba(0,0,0,.09)}
    #liftProductPanelBody{padding:34px 38px;overflow:auto}.lift-product-layout{display:grid;grid-template-columns:minmax(0,.95fr) minmax(0,1.05fr);gap:50px;align-items:center}.lift-image-area{position:relative;min-width:0}.lift-preview-image{height:500px;display:flex;align-items:center;justify-content:center;background:#fff;overflow:hidden;padding:20px;box-sizing:border-box;cursor:zoom-in;position:relative}.lift-preview-image>img{max-width:100%;max-height:100%;width:auto;height:auto;object-fit:contain;display:block}.lift-zoom-hint{position:absolute;bottom:10px;left:50%;transform:translateX(-50%);background:rgba(17,17,17,.76);color:#fff;border-radius:18px;padding:7px 12px;font-size:12px;pointer-events:none;opacity:0;transition:opacity .2s}.lift-preview-image:hover .lift-zoom-hint{opacity:1}.lift-zoom-pane{position:absolute;inset:10px;z-index:5;background:#fff no-repeat;border:1px solid #eee;border-radius:12px;box-shadow:0 10px 30px rgba(0,0,0,.14);opacity:0;visibility:hidden;pointer-events:none;transition:opacity .12s;background-size:220%}.lift-zoom-pane.active{opacity:1;visibility:visible}
    .lift-preview-info{min-width:0;padding:12px 6px 12px 0}.lift-preview-brand{font-size:12px;letter-spacing:.13em;text-transform:uppercase;color:#8b8b8b;margin-bottom:9px;font-weight:700}.lift-preview-title{font-size:clamp(27px,2.55vw,38px);line-height:1.08;font-weight:750;margin:0 0 10px;color:#151515;letter-spacing:-.025em}.lift-preview-presentation{font-size:14px;color:#888;margin:0 0 18px}.lift-preview-price{font-size:28px;line-height:1.1;font-weight:800;margin:0 0 14px;color:#111}.lift-preview-promos{display:flex;flex-wrap:wrap;gap:8px;margin:0 0 18px}.lift-promo-chip{display:inline-flex;align-items:center;gap:6px;border-radius:18px;padding:7px 11px;font-size:12.5px;font-weight:750;line-height:1.15}.lift-promo-offer{background:#fff1e8;color:#9a3e00}.lift-promo-gift{background:#eef8f1;color:#176b3a}.lift-preview-divider{height:1px;background:#ececec;margin:0 0 20px}
    .lift-preview-actions{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin:0 0 22px}.lift-action{min-height:52px;border-radius:26px;border:1.5px solid #111;font-size:15px;font-weight:750;display:flex;align-items:center;justify-content:center;cursor:pointer;transition:transform .16s ease,background .16s ease}.lift-action:hover{transform:translateY(-1px)}.lift-action-cart{background:#fff;color:#111}.lift-action-cart:hover{background:#f5f5f5}.lift-action-buy{background:#111;color:#fff}.lift-action-buy:hover{background:#282828}
    .lift-info-block{border-top:1px solid #ececec;padding:16px 0;font-size:13.5px;line-height:1.55;color:#666}.lift-info-row{display:flex;gap:9px;margin:2px 0}.lift-info-icon{width:20px;flex:0 0 20px;font-size:16px}.lift-info-label{font-weight:750;color:#222}.lift-shipping-note{font-size:12.5px;color:#929292;margin-top:7px}.lift-category{display:inline-block;background:#f4f4f4;border-radius:16px;padding:6px 11px;color:#333;font-weight:650;font-size:13px}body.lift-offcanvas-open{overflow:hidden!important}
    @media(max-width:760px){.card:hover,[class*="product-card"]:hover{transform:none;box-shadow:inherit!important}#liftProductOffcanvas{width:94vw;max-height:94dvh;border-radius:16px}#liftProductPanelBody{padding:22px 18px 24px}.lift-product-layout{grid-template-columns:1fr;gap:6px}.lift-preview-image{height:280px;padding:8px}.lift-preview-title{font-size:25px}.lift-preview-price{font-size:24px;margin-bottom:13px}.lift-preview-info{padding:0}.lift-preview-actions{grid-template-columns:1fr;gap:9px}.lift-action{min-height:48px}.lift-zoom-hint{opacity:1}.lift-zoom-pane{inset:0;background-size:240%}}
    @media(prefers-reduced-motion:reduce){.card,[class*="product-card"],#liftProductBackdrop,#liftProductOffcanvas{transition:none!important}}
  `;document.head.appendChild(style);
  const backdrop=document.createElement('div');backdrop.id='liftProductBackdrop';const panel=document.createElement('aside');panel.id='liftProductOffcanvas';panel.setAttribute('aria-hidden','true');panel.innerHTML=`<div class="lift-product-panel-head"><strong>Detalle</strong><button id="liftProductClose" type="button" aria-label="Cerrar">×</button></div><div id="liftProductPanelBody"></div>`;document.body.append(backdrop,panel);
  const body=panel.querySelector('#liftProductPanelBody'),closeBtn=panel.querySelector('#liftProductClose');const isInteractive=el=>!!el.closest('button,a,input,select,textarea,label,[role="button"]');const getCard=target=>target.closest('.card,[class*="product-card"]');const firstText=(card,selectors)=>{for(const s of selectors){const t=card.querySelector(s)?.textContent?.trim();if(t)return t}return''};const esc=s=>String(s||'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  function inferCategory(card){const explicit=firstText(card,['.category','.categoria','[class*="category"]','[data-category]']);if(explicit)return explicit;const text=(card.textContent||'').toLowerCase();const rules=[['Creatina',['creatina','creatine','creapure']],['Proteína aislada / limpia',['iso xp','iso100','isolate','isopure','aislada']],['Proteína Whey',['whey','protein']],['Ganador de masa',['mass','gainer','ganador']],['Pre entreno',['pre entreno','preentreno','pre-workout','pre workout']],['EAA',['eaa']],['Aminoácidos',['bcaa','amino']],['Glutamina',['glutamina','glutamine']],['Omega 3',['omega 3','fish oil']],['Colágeno',['colageno','collagen']],['Magnesio',['magnesio','magnesium']]];for(const [cat,terms] of rules)if(terms.some(x=>text.includes(x)))return cat;return'Suplementos'}
  function inferPresentation(title){const matches=String(title||'').match(/\b(?:\d+(?:[.,]\d+)?\s?(?:kg|g|gr|lb|lbs|ml)|\d+\s?(?:serv|servicios|caps|capsulas|cápsulas|tabletas))\b/gi);return matches?[...new Set(matches)].join(' · '):''}
  function getPromos(card){
    const found=[];
    const add=(text,type)=>{const clean=String(text||'').replace(/\s+/g,' ').trim();if(!clean||clean.length>120)return;if(!found.some(x=>x.text.toLowerCase()===clean.toLowerCase()))found.push({text:clean,type})};
    const nodes=[...card.querySelectorAll('[class*="sale"],[class*="promo"],[class*="offer"],[class*="oferta"],[class*="gift"],[class*="regalo"],.badge,[data-promo],[data-offer],[data-gift],[data-regalo]')];
    for(const el of nodes){const text=el.textContent?.trim()||el.getAttribute('data-promo')||el.getAttribute('data-offer')||el.getAttribute('data-gift')||el.getAttribute('data-regalo')||'';const gift=/regalo|gratis|obsequio|gift/i.test(text);const offer=/oferta|sale|descuento|promo|%|2x1|3x2/i.test(text);if(gift||offer)add(text,gift?'gift':'offer')}
    const lines=(card.innerText||'').split(/\n+/).map(x=>x.trim()).filter(Boolean);
    for(const line of lines){if(/regalo|gratis|obsequio/i.test(line))add(line,'gift');else if(/oferta|sale|descuento|promo|\b\d{1,2}%\b|2x1|3x2/i.test(line))add(line,'offer')}
    return found.slice(0,3);
  }
  function setupZoom(wrap,img){if(wrap.querySelector('.lift-zoom-pane'))return;const zoom=document.createElement('div');zoom.className='lift-zoom-pane';zoom.style.backgroundImage=`url("${img.src.replace(/"/g,'%22')}")`;wrap.appendChild(zoom);const hint=document.createElement('div');hint.className='lift-zoom-hint';hint.textContent=matchMedia('(hover:hover)').matches?'Mueve el cursor para ampliar':'Toca para ampliar';wrap.appendChild(hint);const move=e=>{const r=wrap.getBoundingClientRect(),p=e.touches?e.touches[0]:e;let x=(p.clientX-r.left)/r.width*100,y=(p.clientY-r.top)/r.height*100;x=Math.max(0,Math.min(100,x));y=Math.max(0,Math.min(100,y));zoom.style.backgroundPosition=`${x}% ${y}%`};if(matchMedia('(hover:hover)').matches){wrap.addEventListener('mouseenter',()=>zoom.classList.add('active'));wrap.addEventListener('mousemove',move);wrap.addEventListener('mouseleave',()=>zoom.classList.remove('active'))}else{wrap.addEventListener('click',e=>{e.stopPropagation();zoom.classList.toggle('active');move(e)});wrap.addEventListener('touchmove',e=>{if(zoom.classList.contains('active'))move(e)},{passive:true})}}
  function openCard(card){if(!card)return;const sourceImg=card.querySelector('.pic img,img'),title=firstText(card,['.name','.title','.product-name','h3','h4'])||'Producto',price=firstText(card,['.price','.precio','[class*="price"]']),brand=firstText(card,['.brand','.marca','[class*="brand"]']),category=inferCategory(card),presentation=inferPresentation(title),promos=getPromos(card);const cardActions=[...card.querySelectorAll('button,a')];const cartSource=cardActions.find(el=>/agregar|carrito/i.test(`${el.textContent||''} ${el.getAttribute('aria-label')||''} ${el.title||''}`))||cardActions.find(el=>/comprar/i.test(el.textContent||''));const buySource=cardActions.find(el=>/comprar/i.test(el.textContent||''))||cartSource;
    body.innerHTML='';const layout=document.createElement('div');layout.className='lift-product-layout';const imageArea=document.createElement('div');imageArea.className='lift-image-area';const imageWrap=document.createElement('div');imageWrap.className='lift-preview-image';if(sourceImg?.src){const img=document.createElement('img');img.src=sourceImg.src;img.alt=sourceImg.alt||title;img.loading='eager';img.decoding='async';imageWrap.appendChild(img);img.addEventListener('load',()=>setupZoom(imageWrap,img),{once:true});if(img.complete)setupZoom(imageWrap,img)}imageArea.appendChild(imageWrap);
    const promoHtml=promos.length?`<div class="lift-preview-promos">${promos.map(p=>`<span class="lift-promo-chip ${p.type==='gift'?'lift-promo-gift':'lift-promo-offer'}">${p.type==='gift'?'🎁':'🔥'} ${esc(p.text)}</span>`).join('')}</div>`:'';
    const info=document.createElement('div');info.className='lift-preview-info';info.innerHTML=`<div class="lift-preview-brand">${esc(brand)}</div><h2 class="lift-preview-title">${esc(title)}</h2>${presentation?`<div class="lift-preview-presentation">${esc(presentation)}</div>`:''}<div class="lift-preview-price">${esc(price)}</div>${promoHtml}<div class="lift-preview-divider"></div><div class="lift-preview-actions"><button type="button" class="lift-action lift-action-cart">Agregar al carrito</button><button type="button" class="lift-action lift-action-buy">Comprar</button></div><div class="lift-info-block"><div class="lift-info-row"><span class="lift-info-icon">🚚</span><div><span class="lift-info-label">Entrega</span><br>Medellín: mismo día para pedidos confirmados antes de las 2:00 p. m.<br>Nacional: 1–3 días hábiles.</div></div><div class="lift-shipping-note">El valor del envío se calcula según la cantidad de productos y la ciudad.</div></div><div class="lift-info-block"><span class="lift-info-label">Categoría:</span> <span class="lift-category">${esc(category)}</span></div>`;
    const trigger=(source,close)=>{if(!source)return;source.click();if(close)closePanel()};info.querySelector('.lift-action-cart').addEventListener('click',e=>{e.stopPropagation();trigger(cartSource,false)});info.querySelector('.lift-action-buy').addEventListener('click',e=>{e.stopPropagation();trigger(buySource,true)});layout.append(imageArea,info);body.appendChild(layout);panel.classList.add('open');backdrop.classList.add('open');panel.setAttribute('aria-hidden','false');document.body.classList.add('lift-offcanvas-open')}
  function closePanel(){panel.classList.remove('open');backdrop.classList.remove('open');panel.setAttribute('aria-hidden','true');document.body.classList.remove('lift-offcanvas-open')}
  document.addEventListener('click',e=>{if(panel.contains(e.target))return;const card=getCard(e.target);if(!card||isInteractive(e.target))return;openCard(card)});closeBtn.addEventListener('click',closePanel);backdrop.addEventListener('click',closePanel);document.addEventListener('keydown',e=>{if(e.key==='Escape'&&panel.classList.contains('open'))closePanel()});
})();